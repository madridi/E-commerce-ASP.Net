@model IEnumerable<ECommerceVersionFinal.LigneCommande>
@using Microsoft.AspNet.Identity;
@using Microsoft.AspNet.Identity.EntityFramework;
@using ECommerceVersionFinal;
@using ECommerceVersionFinal.Models;
@{
    ViewBag.Title = "Index";
    int i = -1;
}

<h2>Liste des produits</h2>



</div>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Produit.nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.quantite)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.prix)
        </th>
        
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    i++;
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Produit.nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.quantite)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.prix)
        </td>
        
        
        <td>
            @Html.ActionLink("Edit", "EditLine", new { id = 88 }) |
        
            @Html.ActionLink("Delete", "Delete2", new { id=88})
        </td>
    </tr>
}
    
</table>

<button class="btn btn-default">@Html.ActionLink("Passer la Commande", "Confirmer")</button>